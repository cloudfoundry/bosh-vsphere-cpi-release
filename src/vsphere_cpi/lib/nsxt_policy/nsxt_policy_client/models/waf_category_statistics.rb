=begin
#NSX-T Data Center Policy API

#VMware NSX-T Data Center Policy REST API

OpenAPI spec version: 2.5.0.0.0

Generated by: https://github.com/swagger-api/swagger-codegen.git
Swagger Codegen version: 2.4.4

=end

require 'date'

module NSXTPolicyClient
  # Statistics of Web Application Firewall (WAF) category
  class WAFCategoryStatistics
    # Category identifier
    attr_accessor :category_id

    # Category name
    attr_accessor :category_name

    # Indicates how often this rule is contributing towards HTTP requests to be denied. Specifically, it indicates how many times this rule is hit (evaluated to TRUE) when the final WAF result is to deny the HTTP request. 
    attr_accessor :deny_hits

    # Indicates how big of an impact this rule has overall in causing HTTP requests to be denied. Range of possible values is [0 - 100] where 0 indicates no impact and 100 indicates very high impact. 
    attr_accessor :deny_score

    # Indicates how many rules for this category are evaluated. If multiple rules are evaluated for the same HTTP request, then it is incremented for each such rule evaluation. If the category contains control (or hidden) rules used for implementation convenience, they are not counted 
    attr_accessor :evaluations

    # Indicates how many rules for this category are hit (evaluated to TRUE). If multiple rules evaluate to TRUE for the same HTTP request, then it is incremented for each such rule hit. If the category contains control (or hidden) rules used for implementation convenience, they are not counted. 
    attr_accessor :hits

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'category_id' => :'category_id',
        :'category_name' => :'category_name',
        :'deny_hits' => :'deny_hits',
        :'deny_score' => :'deny_score',
        :'evaluations' => :'evaluations',
        :'hits' => :'hits'
      }
    end

    # Attribute type mapping.
    def self.swagger_types
      {
        :'category_id' => :'String',
        :'category_name' => :'String',
        :'deny_hits' => :'Integer',
        :'deny_score' => :'Integer',
        :'evaluations' => :'Integer',
        :'hits' => :'Integer'
      }
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      return unless attributes.is_a?(Hash)

      # convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h| h[k.to_sym] = v }

      if attributes.has_key?(:'category_id')
        self.category_id = attributes[:'category_id']
      end

      if attributes.has_key?(:'category_name')
        self.category_name = attributes[:'category_name']
      end

      if attributes.has_key?(:'deny_hits')
        self.deny_hits = attributes[:'deny_hits']
      end

      if attributes.has_key?(:'deny_score')
        self.deny_score = attributes[:'deny_score']
      end

      if attributes.has_key?(:'evaluations')
        self.evaluations = attributes[:'evaluations']
      end

      if attributes.has_key?(:'hits')
        self.hits = attributes[:'hits']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @category_name.nil?
        invalid_properties.push('invalid value for "category_name", category_name cannot be nil.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @category_name.nil?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          category_id == o.category_id &&
          category_name == o.category_name &&
          deny_hits == o.deny_hits &&
          deny_score == o.deny_score &&
          evaluations == o.evaluations &&
          hits == o.hits
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Fixnum] Hash code
    def hash
      [category_id, category_name, deny_hits, deny_score, evaluations, hits].hash
    end

# Builds the object from hash
# @param [Hash] attributes Model attributes in the form of hash
# @return [Object] Returns the model itself
def build_from_hash(attributes)
  return nil unless attributes.is_a?(Hash)
  self.class.swagger_types.each_pair do |key, type|
    if type =~ /\AArray<(.*)>/i
      # check to ensure the input is an array given that the the attribute
      # is documented as an array but the input is not
      if attributes[self.class.attribute_map[key]].is_a?(Array)
        self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
      end
    elsif !attributes[self.class.attribute_map[key]].nil?
      self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
    end # or else data not found in attributes(hash), not an issue as the data can be optional
  end

  self
end

# Deserializes the data based on type
# @param string type Data type
# @param string value Value to be deserialized
# @return [Object] Deserialized data
def _deserialize(type, value)
  case type.to_sym
  when :DateTime
    DateTime.parse(value)
  when :Date
    Date.parse(value)
  when :String
    value.to_s
  when :Integer
    value.to_i
  when :Float
    value.to_f
  when :BOOLEAN
    if value.to_s =~ /\A(true|t|yes|y|1)\z/i
      true
    else
      false
    end
  when :Object
    # generic object (usually a Hash), return directly
    value
  when /\AArray<(?<inner_type>.+)>\z/
    inner_type = Regexp.last_match[:inner_type]
    value.map { |v| _deserialize(inner_type, v) }
  when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
    k_type = Regexp.last_match[:k_type]
    v_type = Regexp.last_match[:v_type]
    {}.tap do |hash|
      value.each do |k, v|
        hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
      end
    end
  else # model
    # If value has resource_type - use it to deserialize
    unless value[:resource_type].nil?
      type = value[:resource_type].to_sym
    end
    temp_model = NSXTPolicyClient.const_get(type).new
    temp_model.build_from_hash(value)
  end
end

# Returns the string representation of the object
# @return [String] String presentation of the object
def to_s
  to_hash.to_s
end

# to_body is an alias to to_hash (backward compatibility)
# @return [Hash] Returns the object in the form of hash
def to_body
  to_hash
end

# Returns the object in the form of hash
# @return [Hash] Returns the object in the form of hash
def to_hash
  hash = {}
  self.class.attribute_map.each_pair do |attr, param|
    value = self.send(attr)
    next if value.nil?
    hash[param] = _to_hash(value)
  end
  hash
end

# Outputs non-array value in the form of hash
# For object, use to_hash. Otherwise, just return the value
# @param [Object] value Any valid value
# @return [Hash] Returns the value in the form of hash
def _to_hash(value)
  if value.is_a?(Array)
    value.compact.map { |v| _to_hash(v) }
  elsif value.is_a?(Hash)
    {}.tap do |hash|
      value.each { |k, v| hash[k] = _to_hash(v) }
    end
  elsif value.respond_to? :to_hash
    value.to_hash
  else
    value
  end
end

  end
end
