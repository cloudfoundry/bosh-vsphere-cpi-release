=begin
#NSX Policy API

#VMware NSX Policy REST API

OpenAPI spec version: 9.0.0.0

Generated by: https://github.com/swagger-api/swagger-codegen.git
Swagger Codegen version: 2.4.46

=end

require 'date'

module Nsxt9PolicyClient
  # VPC Subnet Advanced Configuration 
  class SubnetAdvancedConfig
    attr_accessor :static_ip_allocation

    # Connectivity status of the subnet from other subnets to the VPC. 
    attr_accessor :connectivity_state

    # An array of gateway addresses per address family. Addresses should be in ip/prefix_length format. 
    attr_accessor :gateway_addresses

    # An array of dhcp server addresses per address family. Addresses should be in ip/prefix_length format. 
    attr_accessor :dhcp_server_addresses

    # This property could be used for vendor specific configuration in key value string pairs. 
    attr_accessor :extra_configs

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'static_ip_allocation' => :'static_ip_allocation',
        :'connectivity_state' => :'connectivity_state',
        :'gateway_addresses' => :'gateway_addresses',
        :'dhcp_server_addresses' => :'dhcp_server_addresses',
        :'extra_configs' => :'extra_configs'
      }
    end

    # Attribute type mapping.
    def self.swagger_types
      {
        :'static_ip_allocation' => :'StaticIpAllocation',
        :'connectivity_state' => :'String',
        :'gateway_addresses' => :'Array<String>',
        :'dhcp_server_addresses' => :'Array<String>',
        :'extra_configs' => :'Array<SubnetExtraConfig>'
      }
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      return unless attributes.is_a?(Hash)

      # convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h| h[k.to_sym] = v }

      if attributes.has_key?(:'static_ip_allocation')
        self.static_ip_allocation = attributes[:'static_ip_allocation']
      end

      if attributes.has_key?(:'connectivity_state')
        self.connectivity_state = attributes[:'connectivity_state']
      else
        self.connectivity_state = 'CONNECTED'
      end

      if attributes.has_key?(:'gateway_addresses')
        if (value = attributes[:'gateway_addresses']).is_a?(Array)
          self.gateway_addresses = value
        end
      end

      if attributes.has_key?(:'dhcp_server_addresses')
        if (value = attributes[:'dhcp_server_addresses']).is_a?(Array)
          self.dhcp_server_addresses = value
        end
      end

      if attributes.has_key?(:'extra_configs')
        if (value = attributes[:'extra_configs']).is_a?(Array)
          self.extra_configs = value
        end
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      connectivity_state_validator = EnumAttributeValidator.new('String', ['CONNECTED', 'DISCONNECTED'])
      return false unless connectivity_state_validator.valid?(@connectivity_state)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] connectivity_state Object to be assigned
    def connectivity_state=(connectivity_state)
      validator = EnumAttributeValidator.new('String', ['CONNECTED', 'DISCONNECTED'])
      unless validator.valid?(connectivity_state)
        fail ArgumentError, 'invalid value for "connectivity_state", must be one of #{validator.allowable_values}.'
      end
      @connectivity_state = connectivity_state
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          static_ip_allocation == o.static_ip_allocation &&
          connectivity_state == o.connectivity_state &&
          gateway_addresses == o.gateway_addresses &&
          dhcp_server_addresses == o.dhcp_server_addresses &&
          extra_configs == o.extra_configs
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Fixnum] Hash code
    def hash
      [static_ip_allocation, connectivity_state, gateway_addresses, dhcp_server_addresses, extra_configs].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.swagger_types.each_pair do |key, type|
        if type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end # or else data not found in attributes(hash), not an issue as the data can be optional
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :DateTime
        DateTime.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :BOOLEAN
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        temp_model = Nsxt9PolicyClient.const_get(type).new
        temp_model.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        next if value.nil?
        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end
end
