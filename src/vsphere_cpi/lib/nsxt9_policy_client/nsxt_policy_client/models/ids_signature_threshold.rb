=begin
#NSX Policy API

#VMware NSX Policy REST API

OpenAPI spec version: 9.0.0.0

Generated by: https://github.com/swagger-api/swagger-codegen.git
Swagger Codegen version: 2.4.46

=end

require 'date'

module Nsxt9PolicyClient
  # Threshold values for IDS signature can be used to control IDS event generation frequency. 
  class IdsSignatureThreshold
    # THRESHOLD - An IDS event is generated if signature is hit at least 'count' times within specified time period. LIMIT - At most 'count' number of IDS events are generated for this signature within specified time period. BOTH - A single IDS event is generated if signature is hit at least 'count' times within specified time period. 
    attr_accessor :threshold_type

    # Number of signature hits for threshold. 
    attr_accessor :count

    # Time period (in seconds) for signature threshold. 
    attr_accessor :time_period

    # This property is read-only and shows existing track by value from the signature. SOURCE - Track signature matches for threshold by source. DESTINATION - Track signature matches for threshold by destination. 
    attr_accessor :track_by

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'threshold_type' => :'threshold_type',
        :'count' => :'count',
        :'time_period' => :'time_period',
        :'track_by' => :'track_by'
      }
    end

    # Attribute type mapping.
    def self.swagger_types
      {
        :'threshold_type' => :'String',
        :'count' => :'Integer',
        :'time_period' => :'Integer',
        :'track_by' => :'String'
      }
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      return unless attributes.is_a?(Hash)

      # convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h| h[k.to_sym] = v }

      if attributes.has_key?(:'threshold_type')
        self.threshold_type = attributes[:'threshold_type']
      end

      if attributes.has_key?(:'count')
        self.count = attributes[:'count']
      end

      if attributes.has_key?(:'time_period')
        self.time_period = attributes[:'time_period']
      end

      if attributes.has_key?(:'track_by')
        self.track_by = attributes[:'track_by']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @threshold_type.nil?
        invalid_properties.push('invalid value for "threshold_type", threshold_type cannot be nil.')
      end

      if @count.nil?
        invalid_properties.push('invalid value for "count", count cannot be nil.')
      end

      if @count > 60
        invalid_properties.push('invalid value for "count", must be smaller than or equal to 60.')
      end

      if @count < 1
        invalid_properties.push('invalid value for "count", must be greater than or equal to 1.')
      end

      if @time_period.nil?
        invalid_properties.push('invalid value for "time_period", time_period cannot be nil.')
      end

      if @time_period > 3600
        invalid_properties.push('invalid value for "time_period", must be smaller than or equal to 3600.')
      end

      if @time_period < 1
        invalid_properties.push('invalid value for "time_period", must be greater than or equal to 1.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @threshold_type.nil?
      threshold_type_validator = EnumAttributeValidator.new('String', ['THRESHOLD', 'LIMIT', 'BOTH'])
      return false unless threshold_type_validator.valid?(@threshold_type)
      return false if @count.nil?
      return false if @count > 60
      return false if @count < 1
      return false if @time_period.nil?
      return false if @time_period > 3600
      return false if @time_period < 1
      track_by_validator = EnumAttributeValidator.new('String', ['SOURCE', 'DESTINATION'])
      return false unless track_by_validator.valid?(@track_by)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] threshold_type Object to be assigned
    def threshold_type=(threshold_type)
      validator = EnumAttributeValidator.new('String', ['THRESHOLD', 'LIMIT', 'BOTH'])
      unless validator.valid?(threshold_type)
        fail ArgumentError, 'invalid value for "threshold_type", must be one of #{validator.allowable_values}.'
      end
      @threshold_type = threshold_type
    end

    # Custom attribute writer method with validation
    # @param [Object] count Value to be assigned
    def count=(count)
      if count.nil?
        fail ArgumentError, 'count cannot be nil'
      end

      if count > 60
        fail ArgumentError, 'invalid value for "count", must be smaller than or equal to 60.'
      end

      if count < 1
        fail ArgumentError, 'invalid value for "count", must be greater than or equal to 1.'
      end

      @count = count
    end

    # Custom attribute writer method with validation
    # @param [Object] time_period Value to be assigned
    def time_period=(time_period)
      if time_period.nil?
        fail ArgumentError, 'time_period cannot be nil'
      end

      if time_period > 3600
        fail ArgumentError, 'invalid value for "time_period", must be smaller than or equal to 3600.'
      end

      if time_period < 1
        fail ArgumentError, 'invalid value for "time_period", must be greater than or equal to 1.'
      end

      @time_period = time_period
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] track_by Object to be assigned
    def track_by=(track_by)
      validator = EnumAttributeValidator.new('String', ['SOURCE', 'DESTINATION'])
      unless validator.valid?(track_by)
        fail ArgumentError, 'invalid value for "track_by", must be one of #{validator.allowable_values}.'
      end
      @track_by = track_by
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          threshold_type == o.threshold_type &&
          count == o.count &&
          time_period == o.time_period &&
          track_by == o.track_by
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Fixnum] Hash code
    def hash
      [threshold_type, count, time_period, track_by].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.swagger_types.each_pair do |key, type|
        if type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end # or else data not found in attributes(hash), not an issue as the data can be optional
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :DateTime
        DateTime.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :BOOLEAN
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        temp_model = Nsxt9PolicyClient.const_get(type).new
        temp_model.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        next if value.nil?
        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end
end
