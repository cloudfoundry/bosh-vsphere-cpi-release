=begin
#NSX Policy API

#VMware NSX Policy REST API

OpenAPI spec version: 9.0.0.0

Generated by: https://github.com/swagger-api/swagger-codegen.git
Swagger Codegen version: 2.4.46

=end

require 'date'

module Nsxt9PolicyClient
  # Describes a config item for System Health profile. 
  class ShaPredefinedPluginProfileData
    # The interval of plugin to check the status.
    attr_accessor :check_interval

    # The interval of plugin to report the status.
    attr_accessor :report_interval

    # The smallest report interval if the status is changed. The value of smallest_report_interval_if_change should be less than the value of report_interval 
    attr_accessor :smallest_report_interval_if_change

    # Whether crash the component which spew too much log
    attr_accessor :desired_crash

    # The expected rotation of logging
    attr_accessor :desired_duration

    # The expected rotation for each log
    attr_accessor :granular_desired_duration

    # The threshold to alarm logging report
    attr_accessor :threshold

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'check_interval' => :'check_interval',
        :'report_interval' => :'report_interval',
        :'smallest_report_interval_if_change' => :'smallest_report_interval_if_change',
        :'desired_crash' => :'desired_crash',
        :'desired_duration' => :'desired_duration',
        :'granular_desired_duration' => :'granular_desired_duration',
        :'threshold' => :'threshold'
      }
    end

    # Attribute type mapping.
    def self.swagger_types
      {
        :'check_interval' => :'Integer',
        :'report_interval' => :'Integer',
        :'smallest_report_interval_if_change' => :'Integer',
        :'desired_crash' => :'BOOLEAN',
        :'desired_duration' => :'Integer',
        :'granular_desired_duration' => :'String',
        :'threshold' => :'Integer'
      }
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      return unless attributes.is_a?(Hash)

      # convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h| h[k.to_sym] = v }

      if attributes.has_key?(:'check_interval')
        self.check_interval = attributes[:'check_interval']
      end

      if attributes.has_key?(:'report_interval')
        self.report_interval = attributes[:'report_interval']
      end

      if attributes.has_key?(:'smallest_report_interval_if_change')
        self.smallest_report_interval_if_change = attributes[:'smallest_report_interval_if_change']
      end

      if attributes.has_key?(:'desired_crash')
        self.desired_crash = attributes[:'desired_crash']
      end

      if attributes.has_key?(:'desired_duration')
        self.desired_duration = attributes[:'desired_duration']
      end

      if attributes.has_key?(:'granular_desired_duration')
        self.granular_desired_duration = attributes[:'granular_desired_duration']
      end

      if attributes.has_key?(:'threshold')
        self.threshold = attributes[:'threshold']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          check_interval == o.check_interval &&
          report_interval == o.report_interval &&
          smallest_report_interval_if_change == o.smallest_report_interval_if_change &&
          desired_crash == o.desired_crash &&
          desired_duration == o.desired_duration &&
          granular_desired_duration == o.granular_desired_duration &&
          threshold == o.threshold
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Fixnum] Hash code
    def hash
      [check_interval, report_interval, smallest_report_interval_if_change, desired_crash, desired_duration, granular_desired_duration, threshold].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.swagger_types.each_pair do |key, type|
        if type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end # or else data not found in attributes(hash), not an issue as the data can be optional
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :DateTime
        DateTime.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :BOOLEAN
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        temp_model = Nsxt9PolicyClient.const_get(type).new
        temp_model.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        next if value.nil?
        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end
end
