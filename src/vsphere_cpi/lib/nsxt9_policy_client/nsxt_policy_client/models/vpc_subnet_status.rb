=begin
#NSX Policy API

#VMware NSX Policy REST API

OpenAPI spec version: 9.0.0.0

Generated by: https://github.com/swagger-api/swagger-codegen.git
Swagger Codegen version: 2.4.46

=end

require 'date'

module Nsxt9PolicyClient
  # Realized state of the VPC Subnet, this will show user information related to ip pool, dhcp etc. 
  class VpcSubnetStatus
    # Static IP Pool used to allocate IPs. For ex, for DHCP static binding, LB VIP etc. This will also have pool usage inforamation. 
    attr_accessor :static_ip_pool_path

    # Static IP address ranges used for IP allocation. 
    attr_accessor :static_ip_pool_range

    # Gateway address of the VPC Subnet
    attr_accessor :gateway_address

    # Network address of the VPC Subnet
    attr_accessor :network_address

    # Dhcp server IP address. 
    attr_accessor :dhcp_server_address

    # DHCP address ranges are used for dynamic IP allocation. Existing values cannot be deleted or modified 
    attr_accessor :dhcp_ranges

    # This defines the IP address type that will be allocated for VPC Subnets. 
    attr_accessor :ip_address_type

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'static_ip_pool_path' => :'static_ip_pool_path',
        :'static_ip_pool_range' => :'static_ip_pool_range',
        :'gateway_address' => :'gateway_address',
        :'network_address' => :'network_address',
        :'dhcp_server_address' => :'dhcp_server_address',
        :'dhcp_ranges' => :'dhcp_ranges',
        :'ip_address_type' => :'ip_address_type'
      }
    end

    # Attribute type mapping.
    def self.swagger_types
      {
        :'static_ip_pool_path' => :'String',
        :'static_ip_pool_range' => :'String',
        :'gateway_address' => :'String',
        :'network_address' => :'String',
        :'dhcp_server_address' => :'String',
        :'dhcp_ranges' => :'String',
        :'ip_address_type' => :'String'
      }
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      return unless attributes.is_a?(Hash)

      # convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h| h[k.to_sym] = v }

      if attributes.has_key?(:'static_ip_pool_path')
        self.static_ip_pool_path = attributes[:'static_ip_pool_path']
      end

      if attributes.has_key?(:'static_ip_pool_range')
        self.static_ip_pool_range = attributes[:'static_ip_pool_range']
      end

      if attributes.has_key?(:'gateway_address')
        self.gateway_address = attributes[:'gateway_address']
      end

      if attributes.has_key?(:'network_address')
        self.network_address = attributes[:'network_address']
      end

      if attributes.has_key?(:'dhcp_server_address')
        self.dhcp_server_address = attributes[:'dhcp_server_address']
      end

      if attributes.has_key?(:'dhcp_ranges')
        self.dhcp_ranges = attributes[:'dhcp_ranges']
      end

      if attributes.has_key?(:'ip_address_type')
        self.ip_address_type = attributes[:'ip_address_type']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      ip_address_type_validator = EnumAttributeValidator.new('String', ['IPV4', 'IPV6'])
      return false unless ip_address_type_validator.valid?(@ip_address_type)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] ip_address_type Object to be assigned
    def ip_address_type=(ip_address_type)
      validator = EnumAttributeValidator.new('String', ['IPV4', 'IPV6'])
      unless validator.valid?(ip_address_type)
        fail ArgumentError, 'invalid value for "ip_address_type", must be one of #{validator.allowable_values}.'
      end
      @ip_address_type = ip_address_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          static_ip_pool_path == o.static_ip_pool_path &&
          static_ip_pool_range == o.static_ip_pool_range &&
          gateway_address == o.gateway_address &&
          network_address == o.network_address &&
          dhcp_server_address == o.dhcp_server_address &&
          dhcp_ranges == o.dhcp_ranges &&
          ip_address_type == o.ip_address_type
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Fixnum] Hash code
    def hash
      [static_ip_pool_path, static_ip_pool_range, gateway_address, network_address, dhcp_server_address, dhcp_ranges, ip_address_type].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.swagger_types.each_pair do |key, type|
        if type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end # or else data not found in attributes(hash), not an issue as the data can be optional
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :DateTime
        DateTime.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :BOOLEAN
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        temp_model = Nsxt9PolicyClient.const_get(type).new
        temp_model.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        next if value.nil?
        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end
end
