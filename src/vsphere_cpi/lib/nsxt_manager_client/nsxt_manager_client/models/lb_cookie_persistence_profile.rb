=begin
#NSX-T Manager API

#VMware NSX-T Manager REST API

OpenAPI spec version: 2.5.1.0.0

Generated by: https://github.com/swagger-api/swagger-codegen.git
Swagger Codegen version: 3.0.25
=end

require 'date'

module NSXT
  class LbCookiePersistenceProfile
    # The persistence shared flag identifies whether the persistence table is shared among virtual-servers referring this profile. If persistence shared flag is not set in the cookie persistence profile bound to a virtual server, it defaults to cookie persistence that is private to each virtual server and is qualified by the pool. This is accomplished by load balancer inserting a cookie with name in the format &lt;name&gt;.&lt;virtual_server_id&gt;.&lt;pool_id&gt;. If persistence shared flag is set in the cookie persistence profile, in cookie insert mode, cookie persistence could be shared across multiple virtual servers that are bound to the same pools. The cookie name would be changed to &lt;name&gt;.&lt;profile-id&gt;.&lt;pool-id&gt;. If persistence shared flag is not set in the sourceIp persistence profile bound to a virtual server, each virtual server that the profile is bound to maintains its own private persistence table. If persistence shared flag is set in the sourceIp persistence profile, all virtual servers the profile is bound to share the same persistence table. If persistence shared flag is not set in the generic persistence profile, the persistence entries are matched and stored in the table which is identified using both virtual server ID and profile ID. If persistence shared flag is set in the generic persistence profile, the persistence entries are matched and stored in the table which is identified using profile ID. It means that virtual servers which consume the same profile in the LbRule with this flag enabled are sharing the same persistence table. 
    attr_accessor :persistence_shared

    # The resource_type property identifies persistence profile type. 
    attr_accessor :resource_type

    # If garble is set to true, cookie value (server IP and port) would be encrypted. If garble is set to false, cookie value would be plain text. 
    attr_accessor :cookie_garble

    # If fallback is true, once the cookie points to a server that is down (i.e. admin state DISABLED or healthcheck state is DOWN), then a new server is selected by default to handle that request. If fallback is false, it will cause the request to be rejected if cookie points to a server 
    attr_accessor :cookie_fallback

    # cookie persistence mode
    attr_accessor :cookie_mode

    # HTTP cookie domain could be configured, only available for insert mode. 
    attr_accessor :cookie_domain

    # cookie name
    attr_accessor :cookie_name

    attr_accessor :cookie_time

    # HTTP cookie path could be set, only available for insert mode. 
    attr_accessor :cookie_path

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'persistence_shared' => :'persistence_shared',
        :'resource_type' => :'resource_type',
        :'cookie_garble' => :'cookie_garble',
        :'cookie_fallback' => :'cookie_fallback',
        :'cookie_mode' => :'cookie_mode',
        :'cookie_domain' => :'cookie_domain',
        :'cookie_name' => :'cookie_name',
        :'cookie_time' => :'cookie_time',
        :'cookie_path' => :'cookie_path'
      }
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'persistence_shared' => :'',
        :'resource_type' => :'',
        :'cookie_garble' => :'',
        :'cookie_fallback' => :'',
        :'cookie_mode' => :'',
        :'cookie_domain' => :'',
        :'cookie_name' => :'',
        :'cookie_time' => :'',
        :'cookie_path' => :''
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end
  
    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `NSXT::LbCookiePersistenceProfile` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `NSXT::LbCookiePersistenceProfile`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      # call parent's initialize
      super(attributes)

      if attributes.key?(:'persistence_shared')
        self.persistence_shared = attributes[:'persistence_shared']
      else
        self.persistence_shared = false
      end

      if attributes.key?(:'resource_type')
        self.resource_type = attributes[:'resource_type']
      end

      if attributes.key?(:'cookie_garble')
        self.cookie_garble = attributes[:'cookie_garble']
      else
        self.cookie_garble = true
      end

      if attributes.key?(:'cookie_fallback')
        self.cookie_fallback = attributes[:'cookie_fallback']
      else
        self.cookie_fallback = true
      end

      if attributes.key?(:'cookie_mode')
        self.cookie_mode = attributes[:'cookie_mode']
      else
        self.cookie_mode = 'INSERT'
      end

      if attributes.key?(:'cookie_domain')
        self.cookie_domain = attributes[:'cookie_domain']
      end

      if attributes.key?(:'cookie_name')
        self.cookie_name = attributes[:'cookie_name']
      end

      if attributes.key?(:'cookie_time')
        self.cookie_time = attributes[:'cookie_time']
      end

      if attributes.key?(:'cookie_path')
        self.cookie_path = attributes[:'cookie_path']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = super
      if @resource_type.nil?
        invalid_properties.push('invalid value for "resource_type", resource_type cannot be nil.')
      end

      if @cookie_name.nil?
        invalid_properties.push('invalid value for "cookie_name", cookie_name cannot be nil.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @resource_type.nil?
      resource_type_validator = EnumAttributeValidator.new('', ['LbCookiePersistenceProfile', 'LbSourceIpPersistenceProfile', 'LbGenericPersistenceProfile'])
      return false unless resource_type_validator.valid?(@resource_type)
      cookie_mode_validator = EnumAttributeValidator.new('', ['INSERT', 'PREFIX', 'REWRITE'])
      return false unless cookie_mode_validator.valid?(@cookie_mode)
      return false if @cookie_name.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] resource_type Object to be assigned
    def resource_type=(resource_type)
      validator = EnumAttributeValidator.new('', ['LbCookiePersistenceProfile', 'LbSourceIpPersistenceProfile', 'LbGenericPersistenceProfile'])
      unless validator.valid?(resource_type)
        fail ArgumentError, "invalid value for \"resource_type\", must be one of #{validator.allowable_values}."
      end
      @resource_type = resource_type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] cookie_mode Object to be assigned
    def cookie_mode=(cookie_mode)
      validator = EnumAttributeValidator.new('', ['INSERT', 'PREFIX', 'REWRITE'])
      unless validator.valid?(cookie_mode)
        fail ArgumentError, "invalid value for \"cookie_mode\", must be one of #{validator.allowable_values}."
      end
      @cookie_mode = cookie_mode
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          persistence_shared == o.persistence_shared &&
          resource_type == o.resource_type &&
          cookie_garble == o.cookie_garble &&
          cookie_fallback == o.cookie_fallback &&
          cookie_mode == o.cookie_mode &&
          cookie_domain == o.cookie_domain &&
          cookie_name == o.cookie_name &&
          cookie_time == o.cookie_time &&
          cookie_path == o.cookie_path && super(o)
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [persistence_shared, resource_type, cookie_garble, cookie_fallback, cookie_mode, cookie_domain, cookie_name, cookie_time, cookie_path].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      super(attributes)
      self.class.openapi_types.each_pair do |key, type|
        if type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        elsif attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :DateTime
        DateTime.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        NSXT.const_get(type).build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = super
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end  end
end
